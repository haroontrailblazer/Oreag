[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
[project]
name = "oreag-sdk"
version = "0.1.0"
description = "Typed Oreag API client"
requires-python = ">=3.10"
dependencies = ["httpx>=0.27,<1"]
[tool.setuptools.package-data]
oreag = ["py.typed"]
